{
  "name": "yolk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Desktop-first prototype for a decentralized media platform built with plain TypeScript and CSS.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node server.mjs",
    "start": "node server.mjs",
    "test": "npm run build && node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "playwright-core": "^1.58.2",
    "typescript": "^5.9.2"
  }
}
