{
  "name": "yolk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Desktop-first decentralized media application built with plain TypeScript and CSS.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node server.mjs",
    "app:testflow": "npm run build && node scripts/test-flow-server.mjs --open --port 4173",
    "start": "node server.mjs",
    "test": "npm run build && node --test tests/app.test.mjs && node --test --test-force-exit tests/e2e.test.mjs && node --experimental-test-isolation=none --test --test-force-exit tests/network.test.mjs"
  },
  "devDependencies": {
    "playwright-core": "^1.58.2",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "bittorrent-dht": "^11.0.11",
    "tweetnacl": "^1.0.3",
    "webtorrent": "^2.8.5"
  }
}
